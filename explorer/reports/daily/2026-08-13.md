# PTOAS: VPTO scheduler framework lands; LLVM 19 downgrade branch; nested ui32 scf.if fix; address-recurrence normalization and L1-fill work in flight

Generated: 2026-08-13T11:03:15+00:00
Importance: 9/10

- Summary
  - Upstream main absorbed a large set of VPTO scheduler analysis changes (new IR interface plus Passes), VMI/VPTO bug fixes, and doc updates; base version bumped to v0.59.
  - A branch to downgrade PTOAS to LLVM 19 is active; expect build environment/tooling changes for the bridge.
  - Critical fix: ui32 arith.select is now legalized in VPTO LLVM emission, resolving a nested scf.if regression.
  - New open PR introduces address-recurrence normalization before soft post-update; separate open PRs add L1 fill primitives. A new VMI vexpdif fusion pass is proposed.
  - Issues: two blockers closed (ui32 scf.if select; vci base on camodel), one new A5 int16 vdiv runtime hang reported.
  - AscendNPU-IR: no movement.

- Branch movement (Investigate; score-sorted)
  - PTOAS Markham fork: upstream/main (score 23)
    - Head 04eb37e Fix ui32 arith.select VPTO LLVM lowering; includes prior merges that land the VPTO scheduler framework.
    - Technical: added VPTOScheduling interface and full scheduler analysis scaffolding
      - include/PTO/IR/VPTOScheduling.h; lib/PTO/IR/VPTOScheduling.cpp
      - include/PTO/Transforms/VPTOScheduler/{VPTORegPressureTracker.h, VPTOSchedBoundary.h, VPTOSchedDAG*.h, VPTOSchedModel.h, VPTOSchedRegion.h, VPTOSchedResourceTracker.h}
      - lib/PTO/Transforms/VPTOScheduler/*.cpp and a VPTOSchedulerPass
    - Other notable fixes/tests: VMI scalar stores lowered as point stores; VMI/VPTO tests updated; docs/designs/vpto-scheduler-framework.md added; base version bump to v0.59.
  - PTOAS Markham fork: zhendong/codex/issue-1181 (score 19)
    - Merged main; carries scheduler framework files and VMI fixes (e.g., signless-int handling); substantial test updates.
    - Note: AI branch prefix; treat as non-authoritative unless tied to PRs.
  - PTOAS Markham fork: upstream/codex/downgrade-llvm19 (score 18)
    - Head eb79e5f build: downgrade PTOAS to LLVM 19.
    - Risk: toolchain/API surface change; PR 1156 tracks the migration of sources to MLIR/LLVM 19 and CI/build updates.
  - PTOAS Markham fork: zhendong/codex/scf-while-support (score 18)
    - Merged main; brings in scheduler framework and recent VMI/VPTO fixes alongside ongoing scf.while work.
    - Note: AI branch prefix; non-authoritative.
  - PTOAS Markham fork: zhendong/codex/issue-1179-scalar-cast (score 13)
    - Runtime float<->int casts wired in PTODSL (arith.fptosi/fptoui/sitofp/uitofp); touches VMI/V PTO headers and multiple Transform passes; updates user-guide docs and tests.
  - PTOAS Markham fork: zhendong/codex/vmi-patch-gate (score 11; new branch)
    - Adds PR CI to validate VMI source patching; large AI/skills/doc churn; design-neutral noise unless referenced by core sources.

- GitHub network signals (Investigate/Watch; design-truth still upstream)
  - Multiple direct forks synced the scheduler docs and added CI/skills content; no divergent technical delta beyond upstream was detected.
  - New fork branch (TelGome) proposes a VMI vexpdif fusion pass in Transforms; mirrors GH PR #1246.
  - New yexiaosu branches align with address-recurrence normalization and soft post-update control support; mirrors GH PR #1244.
  - Comm/test tweaks for VPTO SDMA and AsyncSession ABI in a fork; scope limited.

- Issues (new/updated)
  - Closed: #1225 [blocking] VPTO type lowering failed for nested multi-result scf.if carrying ui32 — fixed by legalizing ui32 arith.select in VPTO LLVM emission.
  - Closed: #1092 vci base ignored on camodel; and #1236 MX L1→L0 transpose feature request.
  - Open: #1241 A5 int16 pto.vdiv compiles but hangs at runtime (VPTO LLVM lowering; potential runtime hazard).
  - Open: #1240 masked VMI ops merge mode lacks passthru operand (API design gap).
  - Open: #1242 PTODSL SIMTVF feature asks for per-element vector ctor and loop-unroll hint (surface/API).

- PRs (Investigate highlights)
  - Closed: #1202 VPTO scheduler framework — lands the scheduling DAG, model, and analysis infrastructure; analysis-only within vecscope for this phase.
  - Open: #1156 downgrade to LLVM 19 — switches to vpto-dev LLVM 19; broad build/CI/source adaptations.
  - Closed: #1229 Fix ui32 arith.select VPTO LLVM lowering — applies to default and CANN 9.0 emitters; adds nested scf.if regression test.
  - Open: #1244 VPTONormalizeAddressRecurrencesPass — normalizes loop-varying address recurrences to i16 before soft post-update; centralizes post-update candidate desc; makes normalization reversible; enables soft post-update by default; extensive tests.
  - Open: #1234 and #1235 L1 fill primitives — add PTO/VPTO ops and DSL surfaces for MAT/L1 fills; verify and lower to llvm.hivm SET L1; multiple tests/spec docs. Note potential overlap between the two PRs.
  - Open: #1246 VMI vexpdif fusion — adds a VMIVexpdifFusion pass (f32 on by default; guards for f16 and multiple-use cases) into the VMI semantic pipeline.
  - Closed: #1232 VMI vcvt rounding preservation (incl. Z); #1223 align VMI elementwise type contracts; #1205 lower scalar stores as point stores.
  - Open: #1193 SoftLib sin/cos and i32 vdiv for A5 — late expansion with inline lowering; addresses lack of backend lowering.
  - Open: #1189 unify PTODSL scalar/builtin vector/SIMT semantics — ongoing surface rationalization across docs/IR/generics.

- Risk and bridge impact
  - Scheduler landing: New VPTOScheduling interface and VPTOScheduler passes alter analysis availability and may shift pass ordering contracts. Bridge lowering or scheduling assumptions should be revalidated against include/PTO/IR/VPTOScheduling.h and VPTOScheduler pass APIs.
  - LLVM 19 downgrade: Environment/tooling shift can break local and CI builds; ensure bridge builds and any MLIR API interactions are LLVM 19–compatible.
  - Post-update normalization: The new normalization pass before soft post-update may change address recurrence representation; verify bridge-side address math and post-update expectations.
  - L1 fill ops: New public primitives and lowering paths add surface area; if bridge emits L1 initialization, align with new ops and verification rules.
  - Pipeline changes: VMI vexpdif fusion can alter upstream IR shape pre-bridge; confirm VPTO lowering stability and mask/pmode handling.
  - Runtime hazard: A5 int16 vdiv hang (issue #1241) indicates backend/runtime fragility; avoid emitting that op or rely on SoftLib alternatives where applicable.
  - Noise: Large AI/skills additions in AI-prefixed branches/forks; deprioritize unless referenced by core passes.

- AscendNPU-IR
  - No new branch movement reported. Bridge boundary change pressure is entirely from PTOAS upstream today.

Agent Highlights
- upstream/main: VPTO scheduler framework landed (new VPTOScheduling IR and VPTOScheduler passes); resolves nested ui32 scf.if via arith.select fix; v0.59 tag — high design impact on analysis/scheduling and lowering.
- upstream/codex/downgrade-llvm19: active migration to LLVM 19 — environment/API risk for the bridge; coordinate toolchain.
- zhendong/codex/issue-1181: rebased onto scheduler-containing main; carries VMI fixes and tests — watch for conflicts with bridge assumptions (AI branch, non-authoritative).
- zhendong/codex/scf-while-support: rebased onto main; while/if handling plus scheduler changes — watch (AI branch).
- PR #1244: Address-recurrence normalization before soft post-update — likely to change recurrence semantics; review pass order and expectations.
- PR #1229: ui32 arith.select legalization for VPTO LLVM — unblocks nested scf.if; improves type-lowering robustness.
- PR #1193: SoftLib sin/cos and i32 vdiv on A5 — provides fallback paths where backend lacks lowering.
- Issue #1241: A5 int16 vdiv runtime hang — flag as a do-not-emit pattern or require SoftLib fallback.
