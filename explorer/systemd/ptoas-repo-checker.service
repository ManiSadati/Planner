[Unit]
Description=PTOAS daily repo checker

[Service]
Type=oneshot
WorkingDirectory=/home/m84446336/Planner/explorer
ExecStart=/usr/bin/env bash -lc 'cd /home/m84446336/Planner/explorer && set -a; [ -f .venv ] && . ./.venv; [ -f .env ] && . ./.env; set +a; PYTHONPATH=src python3 -m repo_checker --config config.json'
