diff --git a/triton/backends/ascend/npu_utils.cpp b/triton/backends/ascend/npu_utils.cpp
--- a/triton/backends/ascend/npu_utils.cpp
+++ b/triton/backends/ascend/npu_utils.cpp
@@ -318,6 +318,5 @@ static PyObject *setKernelMaxRegSpillSize(PyObject *self, PyObject *args) {
 static const std::unordered_map<std::string, rtLimitType_t> LimitTypeMap = {
 	{"LOW_POWER_TIMEOUT", rtLimitType_t::RT_LIMIT_TYPE_LOW_POWER_TIMEOUT},
 	{"DVG_WARP_STACK_SIZE", rtLimitType_t::RT_LIMIT_TYPE_SIMT_DVG_WARP_STACK_SIZE},
-	{"WARP_STACK_SIZE", rtLimitType_t::RT_LIMIT_TYPE_SIMT_WARP_STACK_SIZE},
 	{"STACK_SIZE", rtLimitType_t::RT_LIMIT_TYPE_STACK_SIZE}
   };
diff --git a/triton/backends/ascend/compiler.py b/triton/backends/ascend/compiler.py
--- a/triton/backends/ascend/compiler.py
+++ b/triton/backends/ascend/compiler.py
@@ -336,6 +336,9 @@ def get_common_bishengir_compile_options(metadata):
     bishengir_target = metadata['target'].arch
     bishengir_target_opt = f"--target={bishengir_target}"
-    return [bishengir_target_opt]
+    compile_options = [bishengir_target_opt]
+    if os.getenv("TRITON_BISHENGIR_DISABLE_LIB_CALL_NOINLINE") == "1":
+        compile_options.append("--enable-lib-call-no-inline=false")
+    return compile_options
 
 def linalg_to_bin_enable_npu_compile_910_95(linalg: str, metadata, opt):
     linalg, metadata = _parse_linalg_metadata(linalg, metadata)
@@ -459,6 +462,8 @@ def linalg_to_bin_enable_npu_compile_A2_A3(linalg: str, metadata, opt):
         _compile_option_list = [
             f"--target={NPUUtils().get_arch()}",
         ]
+        if os.getenv("TRITON_BISHENGIR_DISABLE_LIB_CALL_NOINLINE") == "1":
+            _compile_option_list.append("--enable-lib-call-no-inline=false")
         multibuffer = metadata["multibuffer"]
         if multibuffer is not None:
             _compile_option_list += [
@@ -469,6 +474,8 @@ def linalg_to_bin_enable_npu_compile_A2_A3(linalg: str, metadata, opt):
             _compile_option_list += [
                 f"--enable-auto-bind-sub-block={enable_auto_bind_sub_block}",
             ]
+        if force_disable_ffts():
+            _compile_option_list += ["--disable-ffts"]
         if _is_ascend_sanitizer_enabled():
             _compile_option_list += ["--enable-sanitizer=true"]
         if not _is_debug_line_info_disabled():
